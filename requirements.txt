numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
statsmodels>=0.14
fpdf2>=2.7

# Optional: only needed for launch_guided_ui() (interactive notebook widgets).
# Not required to run kirin_prototype.py's smoke tests from the command line.
ipywidgets>=8.0
ipython>=8.0
